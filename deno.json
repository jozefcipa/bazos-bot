{
  "tasks": {
    "dev": "denoflare serve bazos-bot --secret-binding TELEGRAM_WEBHOOK_SECRET:\"$TELEGRAM_WEBHOOK_SECRET\" --secret-binding TELEGRAM_API_KEY:\"$TELEGRAM_API_KEY\" --bundle 'backend=esbuild'",
    "deploy": "denoflare push bazos-bot --verbose --bundle 'backend=esbuild'"
  },
  "fmt": {
    "indentWidth": 2,
    "singleQuote": true,
    "semiColons": false
  }
}
